function strLength(a, b, c) {
    let sumLen = a.length + b.length + c.length;
    let average = Math.floor(sumLen / 3);
    console.log(sumLen);
    console.log(average);
}

strLength('chocolate', 'ice cream', 'cake');
